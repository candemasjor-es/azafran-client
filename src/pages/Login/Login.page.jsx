import { Button } from "antd";
const Login = () => {
    return (
        <Flex gap="small" wrap>
            <Button type="primary">Primary Button</Button>
        </Flex>
    );
};

export { Login };
